# Email Configuration

The form handlers call PHP mail(). The config file is protected by /config/.htaccess and ignored by git. Use sales@trinetrafleet.com as MAIL_TO and MAIL_FROM for all website form submissions. Never place SMTP passwords in HTML or JavaScript.
