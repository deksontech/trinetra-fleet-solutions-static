# Email Configuration

The form handlers call PHP mail(). The config file is protected by /config/.htaccess and ignored by git. Use booking@trinetrafleet.com as MAIL_TO and MAIL_FROM. Never place SMTP passwords in HTML or JavaScript.
